import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Evaluacio2Movile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
